import RNConfig from 'react-native-config';

const {
  API_TOKEN,
  API_URL,
  API_IMAGE_ORIGINAL_URL,
  API_IMAGE_CROPPED_URL,
} = RNConfig;

export const AppConfig = {
  API_TOKEN,
  API_URL,
  API_IMAGE_ORIGINAL_URL,
  API_IMAGE_CROPPED_URL,
};
